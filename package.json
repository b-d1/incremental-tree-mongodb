{
  "name": "incremental-merkle-tree-mongo",
  "version": "1.0.0",
  "description": "Incremental merkle tree implementation using Mongoose, suitable for Mongo.db",
  "main": "dist/src/index.js",
  "author": "Blagoj Dimovski",
  "license": "MIT",
  "dependencies": {
    "incrementalquintree": "^1.0.7"
  },
  "devDependencies": {
    "@types/node": "^16.9.4",
    "tslint": "^6.1.3",
    "typescript": "^4.4.3"
  },
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "prestart": "npm run build",
    "start": "node .",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
